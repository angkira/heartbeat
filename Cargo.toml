[package]
name = "heartbeat"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
actix-web = "4.9.0"
config = "0.14.1"

[features]
default = ["config/json", "config/yaml", "config/toml"]
